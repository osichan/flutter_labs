name: flutter_lab
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  torch_light: ^1.1.0
  mqtt_client: ^10.0.0  # Added for MQTT functionality
  connectivity_plus: ^6.0.0  # Added for internet connectivity checks

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true